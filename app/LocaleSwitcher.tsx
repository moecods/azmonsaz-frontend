"use client";

export default function LocaleSwitcher() {
  // This component is now completely hidden and doesn't manipulate DOM
  // RTL is set directly in the HTML template to avoid hydration issues
  return null;
}


